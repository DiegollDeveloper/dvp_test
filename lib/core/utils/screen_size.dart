import 'package:flutter/material.dart';

class ScreenSize {
  static double width(BuildContext context) => context.size!.width;
  static double height(BuildContext context) => context.size!.height;
}
